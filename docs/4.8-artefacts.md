# Artefacts outside the repository

Everything under change control that **leaves no trace in git**: files on the
production host, resources in Oracle Cloud, objects in GitHub, and records held
by services we do not run.

**Why only those.** A repository artefact is self-naming — its path *is* its
name, it either exists or it does not, and `git log` says what happened to it.
None of that is true here. A firewall rule, a bucket or a systemd timer is
invisible to every tool this project has, so **this file is the only record there
will ever be**, which is also why the entries carry enough detail to recreate
them.

That gives this document a second job: it is the answer to *"what else was on
that box?"* after the box is gone.

## How an artefact is named

The same convention wherever an artefact is listed — here, in a project issue's
*impacted artefacts*, or in a design note. Two lists can then be compared for
overlap by matching strings rather than by knowing both projects.

| kind | canonical name | example |
| --- | --- | --- |
| in the repository | its path from the repository root | `docs/3.4-production-environment.md` |
| on a host | the host role, then the absolute path | `production:/etc/systemd/journald.conf.d/50-cap.conf` |
| a cloud resource | provider, type, the provider's own name | `OCI bucket tile-lite-elite-backups` |
| a GitHub object | `github:` then type and name | `github:label folded` |
| an external service | the service, then the thing | `OCI alarm tile-lite-elite-health` |

## The production host

Verified on the VM, 2026-08-21.

| artefact | what it is | why it is not in the repository |
| --- | --- | --- |
| `production:~/tile-lite-elite/docker-compose.yml` | the compose file the running stack uses | **copied there by `deploy.sh`** from the repository — the copy is a deployment artefact, and the source of truth is in git |
| `production:~/tile-lite-elite/snapshots/` | pre-deploy database snapshots, newest five kept | data, created by `deploy.sh` |
| `production:/etc/systemd/journald.conf.d/50-cap.conf` | `SystemMaxUse=50M`. Written 2026-07-30 | applied by hand. It is why journald is *not* on defaults, which #174 discovered the hard way |
| `production:` docker volumes `tile-lite-elite-data`, `-caddy-data`, `-caddy-config` | the database, and Caddy's certificates and state | created by Docker, and the first is the only irreplaceable thing on the machine |
| `production:` iptables `INPUT` chain | accepts established, ICMP, loopback and **22 only**; rejects the rest | applied by hand |

**Two notes worth keeping with the entries.**

Ports 80 and 443 do **not** appear in the `INPUT` chain. Docker publishes them
through its own `DOCKER` chain in the `nat` table, which bypasses `INPUT`
entirely — so reading `iptables -S INPUT` and concluding the site is unreachable
is a mistake waiting to be made twice.

The compose file on the host is a **copy**. Editing it directly changes what runs
until the next deploy overwrites it, silently, which is the worst kind of
configuration drift because it repairs itself just late enough to confuse
whoever is looking.

## Oracle Cloud

| artefact | what it is |
| --- | --- |
| `OCI compute instance-20260712-1225` | the production VM, `uk-cardiff-1`, Always Free tier |
| `OCI bucket tile-lite-elite-backups` | created 2026-08-21 for #174's R2. Standard tier, private, root compartment, namespace `axbbaaptgyab`. Empty but for a 12-byte `probe.txt` left by the credential test that proved a write-only pre-authenticated request cannot delete |
| `OCI` monitoring alarms | production alerting, live since 2026-08-13 (#136) — **details not yet transcribed here** |
| `OCI` external health check | probes `/health` from three regions and mails the owner, live since 2026-08-13 (#136) — **details not yet transcribed here** |

## GitHub

| artefact | what it is |
| --- | --- |
| `github:labels` | two, and only two: `needs-triage` and `folded`. Everything a label used to carry — type, phase, review state — is an issue field, an issue type, or native GitHub |
| `github:issue types` | `Index`, `Workstream`, `Project`, `Requirement` — org-level, set when an issue is raised |
| `github:issue fields` | ours: `Type of change`, `Phase`. GitHub's built-ins, kept: `Priority`, `Effort`, `Start date`, `Target date` |
| `github:ruleset main` | **does not exist yet.** Agreed: require CI's `check` job on `main`, and nothing more |

Workflows live in `.github/workflows/` and are repository artefacts, so they are
not listed here.

## Not yet recorded

Named rather than omitted, because an incomplete list that says where it is
incomplete is useful and one that pretends to be complete is not.

- **DNS** for `tileliteelite.com` and `rehearsal.tileliteelite.com` — the records
  exist and where they are held has not been written down here
- **the rehearsal environment's host artefacts** — the same shape as production's
  and not yet inventoried
- **the OCI alarm and health-check definitions** from #136 — live and working,
  their configuration described in the issue rather than here

## When an entry appears

**When the delivery that creates it completes**, never in anticipation. A project
lists what it *intends* to touch in its own issue; this file records what
*exists*. Keeping those apart is what stops the register becoming a wish list
that nobody trusts.
