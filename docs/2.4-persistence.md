# Persistence

## SQLite File Layout

Each environment has one SQLite database consisting of the main .sqlite3 file and sidecar files, .sqlite3-wal and .sqlite3-shm, created as needed by the database when write-ahead logging is enabled.

Main file:

- local development: `./data/tile-lite-elite.sqlite3`
- self-hosted instance: `/var/lib/tile-lite-elite/tile-lite-elite.sqlite3`

Sidecar files in the same directories:

- `tile-lite-elite.sqlite3-wal`
- `tile-lite-elite.sqlite3-shm`

## What Is Persisted

The database should store durable game and platform data such as:

- active games and finished games
- board state snapshots or move history
- tile bag state and player racks if needed for recovery
- player or session records
- engine metadata and configuration
- match results and audit history
- saved game metadata
- migration version state

## What Stays In Memory

The following should generally stay in memory or be recomputed:

- current UI state
- transient preview calculations
- temporary engine search state
- cached move evaluations that can be rebuilt

## Persistence Model

The server remains authoritative and writes the canonical state to SQLite.

Clients and engine proxies may use the shared rules library for previews, but they do not own persistence.

Good persistence behavior for this project means:

- a match can be resumed after a restart
- finished games remain available for history and replay
- the database can be backed up by copying the single main file, ideally after a clean shutdown or with WAL-aware backup procedures
- the design stays simple enough to run locally or on a free tier

## Recommended Approach

Use migrations to evolve the schema over time.

Prefer simple, explicit tables over complex embedded blobs unless a blob is the clearest way to capture a replay snapshot.

Keep the write path small and predictable so the project stays easy to self-host.

## Retention: how long a game is kept

The rules are RET-1 to RET-4 in [1.0 Rules](1.0-rules.md). This note is how
they are met, and why the shape is what it is.

### The problem

Games are never deleted. Every game ever played stays in the database and,
per issue #29, in memory — `AppState::new` loads all of them at boot. So resident
memory and startup time are proportional to *cumulative* games, not active
ones, and both grow forever.

Measured 2026-08-03: 37 games in production, ~46 KB each. Disk is not the
constraint — that is on the order of a million games before it matters.
Memory is, on a 954 MB box.

### What is deleted, and when

Two clocks, split on whether the game reached an end.

| status | rule | built |
| --- | --- | --- |
| `Finished` / `Aborted` | deleted seven days after it ended | yes |
| `Waiting` / `Active` | thirty-day countdown from last activity, with the creator asked at seven days | no |

A completed game is finished with. Nothing more will happen to it, so the only
question is how long anyone wants to look back at it, and the clock can start
the moment it ends.

A game that has not completed is a different thing: something may yet happen,
and the only way to find out is to ask. `updated_at` carries "last activity"
for it — the field moves whenever anyone joins, declines, changes the roster
or takes a turn, so the countdown only runs on a game nobody has touched at
all, and a creator answering *keep* restarts the same clock rather than
needing one of its own.

The original design was one ninety-day rule for every status, which read as
the simplest thing until the two halves turned out to be asking different
questions.

#### Why an unfinished game gets a question rather than a deadline

A waiting game's seats each carry a `SeatInvitationStatus`, and the
combination says something different about who is waiting for whom:

| situation | what it means | who loses if it is deleted |
| --- | --- | --- |
| every human seat `NotSent` | the creator built a roster and asked nobody | only the creator, who forgot about it |
| any seat `Pending` | someone has been asked and has not replied | an invitee who may still act on it |
| any seat `Rejected` | someone declined; the roster needs attention | the creator, who may not have noticed |
| every seat claimed | everyone accepted; waiting on **Start** | everyone who accepted |

The last is not an invitation state at all — `invitation_status` is `None`
once a seat is claimed — but it is the case where deletion throws away the
most: several people agreed to play and the creator did not press the button.

That row is what the question exists for. A period alone cannot tell the four
apart, and the one that matters most is the one where the creator is the only
person who can act and the only person who has forgotten. Asking costs a
prompt; guessing costs four people their game.

#### Where the question appears

In the games list, as its own section alongside invitations. It is the same
kind of thing: something addressed to this player that is waiting on an answer
from them, and that belongs where they already look for such things rather
than in a notification that passes.

Its three answers are RET-3's. **Delete** is worth having for its own sake —
a creator who knows a game is dead clears it in one action instead of waiting
out the countdown.

### Where the sweep runs

Alongside `sweeps::expire_overdue_turns`, which already iterates **every**
loaded game rather than only the one a handler touched, and is called at the
top of any handler that reads live games.

That gives a useful property: **growth requires use, and use triggers the
sweep.** A server nobody plays accumulates nothing; one being played cleans
itself on every request. No background scheduler — which this server
deliberately does not have.

### What survives

`player_ratings` and `rating_history` are separate tables, so a player's
rating and its history outlive the games they came from. That is what makes
deleting games acceptable at all.

**To confirm when implementing**: that no statistic is computed by counting
game rows. If one is, retention would silently change players' numbers.

### Open questions

- **No export.** Deletion is irreversible and there is no archive. Worth
  deciding whether an admin command should be able to export before the sweep
  reaches something.
- **The countdown is shorter than the longest move time limit.** A limit may
  be set up to forty days, so a game whose player is legitimately still on
  turn can reach thirty days of no activity and be deleted with time left on
  their clock. Either the countdown pauses while a move is genuinely pending,
  or the limit is capped below thirty days, or *keep* is expected to carry it.

### Why this rather than #29's proposal

Issue #29 suggested lazy loading or eviction so memory stops tracking all-time
games. Retention is better: it bounds the *set*, so memory, startup time and
disk all follow from one change. #29 becomes worth doing only if the retained
set is itself too large — at ~46 KB per game, not for years.
