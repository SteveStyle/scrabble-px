# TEMPORARY PROBE — delete after #323 is answered.
#
# The other candidate spelling: `parent_issue_added` as an `issues` type rather
# than its own event. Separate file so that one being rejected does not take the
# other with it.
name: probe issues parent_issue_added
on:
  issues:
    types: [parent_issue_added]
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "issues.parent_issue_added fired for #${{ github.event.issue.number }}"
